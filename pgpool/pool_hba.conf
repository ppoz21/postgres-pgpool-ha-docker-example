# pool_hba.conf is present for completeness. Authentication is disabled in
# pgpool.conf for this local lab, while PostgreSQL containers use trust auth
# inside the isolated Docker network.
host all all 0.0.0.0/0 trust
host all all ::/0 trust
